1.NULL — це коли значення немає. Його не можна перевіряти через =. 

2. WHERE відбирає окремі рядки таблиці до групування. HAVING відбирає вже створені групи після групування. 

3.У випадку, якщо в нас дуже велика база даних, варто уникати SELECT *, коли потрібні лише кілька стовпців. Тому що він завантажує всі дані, через що запит може працювати повільніше. 