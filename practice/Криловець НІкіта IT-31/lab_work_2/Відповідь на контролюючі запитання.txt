1.WHERE price = NULL завжди повертає порожній результат, тому що NULL означає відсутність або невідомість значення. 
Для перевірки потрібно використовувати WHERE price IS NULL.

2.WHERE фільтрує окремі рядки таблиці до групування, а HAVING фільтрує вже сформовані групи після групування.

3.SELECT * варто уникати, коли потрібні лише певні стовпці. Явне зазначення стовпців робить запит зрозумілішим, точнішим і не виводить зайві дані.