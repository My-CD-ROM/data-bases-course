Я використовував такі команди як:
SELECT type, duration_days from memberships
.
SELECT * FROM memberships WHERE duration_days > 30 AND price < 1500
.
SELECT * FROM memberships WHERE duration_days > 30 LIMIT 3
.
SELECT type from memberships WHERE price is NOT NULL
.
SELECT type from memberships WHERE price is NULL
.
SELECT * FROM memberships WHERE duration_days < 31 AND PRICE < 500